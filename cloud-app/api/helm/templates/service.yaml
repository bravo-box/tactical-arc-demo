apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-api.fullname" . }}
  labels:
    {{- include "cloud-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "cloud-api.selectorLabels" . | nindent 4 }}
