# cloud-cluster/helm

Helm chart (`cloud-cluster/`) that deploys the cloud apps and their dependencies.

```bash
helm lint cloud-cluster/helm/cloud-cluster
helm template cloud-cluster cloud-cluster/helm/cloud-cluster
```
