apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cloud-cluster.fullname" . }}-telemetry-api
  labels:
    {{- include "cloud-cluster.labels" . | nindent 4 }}
    app.kubernetes.io/component: telemetry-api
spec:
  replicas: {{ .Values.telemetryApi.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "cloud-cluster.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: telemetry-api
  template:
    metadata:
      annotations:
        checksum/servicebus: {{ .Values.telemetryApi.serviceBus | toJson | sha256sum }}
      labels:
        {{- include "cloud-cluster.labels" . | nindent 8 }}
        app.kubernetes.io/component: telemetry-api
        {{- if .Values.telemetryApi.workloadIdentity.clientId }}
        azure.workload.identity/use: "true"
        {{- end }}
    spec:
      serviceAccountName: {{ include "cloud-cluster.fullname" . }}-telemetry-api
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
      containers:
        - name: telemetry-api
          image: {{ include "cloud-cluster.image" . }}
          imagePullPolicy: {{ .Values.telemetryApi.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: Images__Enabled
              value: {{ .Values.telemetryApi.imageServiceBus.enabled | quote }}
            - name: ServiceBus__Topic
              value: {{ .Values.telemetryApi.serviceBus.topic | quote }}
            - name: ServiceBus__Subscription
              value: {{ .Values.telemetryApi.serviceBus.subscription | quote }}
            - name: ServiceBus__FullyQualifiedNamespace
              value: {{ .Values.telemetryApi.serviceBus.fullyQualifiedNamespace | quote }}
            - name: ImageServiceBus__Topic
              value: {{ .Values.telemetryApi.imageServiceBus.topic | quote }}
            - name: ImageServiceBus__Subscription
              value: {{ .Values.telemetryApi.imageServiceBus.subscription | quote }}
            - name: ImageServiceBus__FullyQualifiedNamespace
              value: {{ .Values.telemetryApi.imageServiceBus.fullyQualifiedNamespace | quote }}
            - name: CameraCommands__Queue
              value: {{ .Values.telemetryApi.cameraCommands.queue | quote }}
            - name: CameraCommands__FullyQualifiedNamespace
              value: {{ .Values.telemetryApi.cameraCommands.fullyQualifiedNamespace | quote }}
            - name: LocationCommands__Enabled
              value: {{ .Values.telemetryApi.locationCommands.enabled | quote }}
            - name: LocationCommands__Queue
              value: {{ .Values.telemetryApi.locationCommands.queue | quote }}
            - name: LocationCommands__FullyQualifiedNamespace
              value: {{ .Values.telemetryApi.locationCommands.fullyQualifiedNamespace | quote }}
            - name: Storage__Container
              value: {{ .Values.telemetryApi.storage.container | quote }}
            - name: Storage__AccountUrl
              value: {{ .Values.telemetryApi.storage.accountUrl | quote }}
            - name: Monitor__ActiveDeviceTimeoutSeconds
              value: {{ .Values.telemetryApi.monitor.activeDeviceTimeoutSeconds | quote }}
            - name: Monitor__MaxHeartbeatsPerDevice
              value: {{ .Values.telemetryApi.monitor.maxHeartbeatsPerDevice | quote }}
            {{- if .Values.telemetryApi.serviceBus.existingSecret }}
            - name: ServiceBus__ConnectionString
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.telemetryApi.serviceBus.existingSecret }}
                  key: {{ .Values.telemetryApi.serviceBus.connectionStringKey }}
            {{- end }}
            {{- if .Values.telemetryApi.imageServiceBus.existingSecret }}
            - name: ImageServiceBus__ConnectionString
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.telemetryApi.imageServiceBus.existingSecret }}
                  key: {{ .Values.telemetryApi.imageServiceBus.connectionStringKey }}
            {{- end }}
            {{- if .Values.telemetryApi.storage.existingSecret }}
            - name: Storage__ConnectionString
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.telemetryApi.storage.existingSecret }}
                  key: {{ .Values.telemetryApi.storage.connectionStringKey }}
            {{- end }}
            {{- if .Values.telemetryApi.cameraCommands.existingSecret }}
            - name: CameraCommands__ConnectionString
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.telemetryApi.cameraCommands.existingSecret }}
                  key: {{ .Values.telemetryApi.cameraCommands.connectionStringKey }}
            {{- end }}
            {{- if .Values.telemetryApi.locationCommands.existingSecret }}
            - name: LocationCommands__ConnectionString
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.telemetryApi.locationCommands.existingSecret }}
                  key: {{ .Values.telemetryApi.locationCommands.connectionStringKey }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          resources:
            {{- toYaml .Values.telemetryApi.resources | nindent 12 }}
