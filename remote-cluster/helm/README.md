# remote-cluster/helm

Helm chart (`remote-cluster/`) for deploying the edge apps to the edge device.

```bash
helm lint remote-cluster/helm/remote-cluster
helm template remote-cluster remote-cluster/helm/remote-cluster
```
